name: Deploy gym app to GitHub Pages

on:
  push:
    branches: [claude/gym-training-progress-app-mt7wwr]
    paths: ['gym-app/**', '.github/workflows/deploy-gym-app.yml']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: gym-app
      - id: deployment
        uses: actions/deploy-pages@v4
